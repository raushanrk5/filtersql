name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: filtersql_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: filtersql_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd "mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: gofmt (all modules)
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then echo "gofmt needed:"; echo "$unformatted"; exit 1; fi

      - name: vet + test — root (zero-dep)
        run: |
          go vet ./...
          go test -race ./...

      - name: test — integration (SQLite + Postgres + MySQL)
        working-directory: integration
        env:
          FILTERSQL_POSTGRES_DSN: postgres://postgres:postgres@localhost:5432/filtersql_test?sslmode=disable
          FILTERSQL_MYSQL_DSN: root:root@tcp(127.0.0.1:3306)/filtersql_test
        run: |
          go vet ./...
          go test -race ./...

      - name: test — cookbook
        working-directory: cookbook
        run: |
          go vet ./...
          go test -race ./...
